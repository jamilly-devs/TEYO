# DOCUMENTATION_AUDIT.md

## Auditoria de correção — entrega para Claude Code

Esta versão foi revisada para eliminar contradições de escopo e status de decisão encontradas na versão anterior.

### Correções obrigatórias realizadas

1. **V1/V2 corrigido**: Notícias e Relatórios foram removidos do escopo V1 e marcados como V2+.
2. **Home corrigida**: a hierarquia confirmada permanece conversa → progresso → tarefas/plano do dia → agenda e demais blocos gerais. Referências históricas a Notícias não liberam sua implementação na V1.
3. **Multiusuário corrigido**: V1 começa com um usuário ativo, mas isolamento por `user_id` e preparação estrutural para múltiplos usuários já são requisitos V1. Experiência completa de multiusuário fica fora do V1.
4. **Qwen corrigido**: Qwen3.5-4B local é decisão fechada da V1; runtime/infraestrutura exatos continuam pendentes.
5. **Definition of Done corrigido**: deixou de exigir CRUD para todo módulo indiscriminadamente e passou a exigir critérios verificáveis das funcionalidades V1.
6. **API/Navegação corrigidas**: endpoints, rotas e telas de Notícias/Relatórios não fazem parte da V1.
7. **Schema corrigido**: `news_preferences` foi retirado do schema funcional V1.
8. **Documentos futuros preservados**: `MODULES/NEWS.md` e `MODULES/REPORTS.md` continuam como especificações futuras, claramente marcadas V2+.

### Pendências legítimas

- Stack de frontend/backend/banco.
- Runtime exato de produção do Qwen3.5-4B.
- Valores do Motor de Padrões.
- Regras exatas de gamificação.
- Estágios exatos do mascote.
- Mecanismo concreto de busca de vagas.
- Algoritmo de priorização do Planejador.
- Segurança/privacidade e schemas que ainda não estejam suficientemente especificados.

### Critério de entrega

A documentação só deve ser considerada fonte segura quando o Claude Code conseguir identificar, sem inferência, o que é V1, o que é V2+, o que é decisão fechada e o que ainda está pendente.

## Decisões registradas durante a implementação

### 2026-09-06 — FASE 5 (Tools)

1. **`remove_market_item` = exclusão definitiva (hard delete).** Resolve a pendência "Semântica final de operações de Mercado" (antes listada em "Pendências legítimas" acima). A decisão já existia implicitamente no código da FASE 2 (`api/routers/market.py`, endpoint `DELETE /market/items/{id}`, comentado como "per Jams's decision" desde aquela fase) — nesta entrada ela é formalizada e sincronizada em `TOOLS.md`, `MODULES/MARKET.md` e `API.md`. Distinta de marcar como comprado (`PATCH .../items/{id}`, `status=purchased`, não remove a linha).
2. **Tools de listagem adicionadas: `list_tasks`, `list_events`, `list_goals`, `list_market_items`, `list_financial_records`.** A lista-base de `TOOLS.md` não tinha nenhuma tool de leitura para esses módulos além de `get_daily_plan` (que só cobre o dia atual e só chega na FASE 8) — sem elas o LLM não teria como resolver referências ambíguas a itens existentes fora da conversa atual. Decisão tomada com Jams em 2026-09-06 (opção "adicionar list_* para todos os módulos"), documentada em `TOOLS.md`.
3. **`complete_task` na FASE 5 não implementa o efeito colateral de gamificação/mascote descrito em `TOOLS.md`.** Esse efeito é FASE 9 (`GAMIFICATION.md`, `MASCOT.md`, ainda vazios em `backend/gamification` e `backend/mascot`); a tool desta fase só marca a tarefa como concluída. Não é uma mudança de escopo — é a mesma sequência de fases já definida em `ROADMAP.md`.
4. **Escopo de tools da FASE 5 limitado aos endpoints da FASE 2** (Tarefas, Agenda, Objetivos, Mercado, Finanças), conforme o próprio texto do `ROADMAP.md` ("conectando Orquestrador aos endpoints da FASE 2"). `get_daily_plan`, `reorganize_day`, `get_user_preferences`, `get_memory`, `get_patterns` e `get_routine_changes` continuam definidas em `TOOLS.md` mas ficam para as FASES 6–8, quando os sistemas de que dependem (Memória, Motor de Padrões, Planejador) existirem.

### 2026-09-07 — FASE 6 (Memória)

1. **Tools de escrita de memória adicionadas: `remember_preference`, `remember_fact`, `forget_memory`.** A lista-base de `TOOLS.md` só tinha `get_user_preferences`/`get_memory` (leitura) — sem uma tool de escrita, `memory_entries` nunca seria criada a partir da conversa, contradizendo `MEMORY.md` ("quando algo é salvo") e `ACCEPTANCE_CRITERIA.md` (remoção de memória precisa funcionar). Decisão tomada com Jams em 2026-09-06 (opção "tools de escrita conversacionais"), documentada em `TOOLS.md`.
2. **`memory_entries`: mesma `key`+`category` do mesmo usuário sobrescreve, não versiona.** Resolve o "A DEFINIR" de `MEMORY.md` sobre atualização de memória contraditória — decisão técnica, sem recomendação prévia no documento; escolhida por consistência com o resto do V1 (nenhuma outra entidade é versionada).
3. **Critério de relevância de `MEMORY.md` resolvido tecnicamente**: preferências vão inteiras ao `Context` a cada turno (poucas, devem moldar o comportamento de forma consistente); fatos/decisões são filtrados por correspondência de palavra com a mensagem atual, limitados a 5 por turno. Documentado em `MEMORY.md` e implementado em `tools/memory.py:select_relevant_context`.
4. **`Context` (memória/preferências/padrões) passou a ser efetivamente usado pelo Adapter.** Até a FASE 5, `OllamaAdapter.generate()` recebia `context` mas nunca o utilizava — o parâmetro existia no contrato (LLM.md) mas não chegava ao modelo. Corrigido nesta fase: memória/preferências relevantes são enviadas como uma mensagem `system` adicional. `context.patterns` continua sem uso — formato fica para a FASE 7 (`PATTERN_ENGINE.md`).

### 2026-09-07 — FASE 7 (Motor de Padrões)

1. **Janela histórica (21 dias) / janela recente (7 dias) e limiares (80% para promover a `active`, 70% de confiança para "mudança possível") confirmados por Jams**, exatamente a OPÇÃO RECOMENDADA de `PATTERN_ENGINE.md` — esse documento exigia confirmação explícita antes da FASE 7 (diferente da maioria dos "A DEFINIR" do projeto, que podem ser adotados provisoriamente). Implementado em `pattern_engine/task_time_of_day.py`.
2. **Escopo de dado real limitado a `tasks` (horário de conclusão por categoria).** `habit_logs`, `pattern_events` e `productivity_logs` seguem sem nenhum escritor no código (Hábitos e Pomodoro ainda não têm módulo construído) — por decisão desta fase, as tools/endpoints das FASES 2–6 não foram alteradas para começar a alimentá-los. Segue exatamente o texto do `ROADMAP.md` ("Motor de Padrões... sobre o histórico já existente das FASES 2–6"). Documentado em `PATTERN_ENGINE.md`.
3. **Gatilho de execução: sob demanda, sem worker/job periódico.** `FLOWS.md` item 7 deixava isso "A DEFINIR" (gatilho exato) mas sem OPÇÃO RECOMENDADA nem gate de aprovação — decisão técnica, já que não há infraestrutura de scheduler no projeto (`requirements.txt` só tem fastapi/uvicorn/sqlalchemy/alembic/httpx) e a V1 é single-user local. Toda chamada de `get_patterns`/`get_routine_changes`, ou montagem de contexto do turno, recomputa e sincroniza `patterns`.
4. **Rebaixamento (`active`→`deprecated`) não cria uma linha `candidate` separada para o período emergente.** `PATTERN_ENGINE.md` menciona "um novo candidate pode começar a ser observado" junto da despromoção; como o schema de `patterns` (FASE 1) não tem uma coluna para o "período" além do texto de `description`, optou-se por manter uma única linha por (usuário, categoria) e comunicar o padrão emergente só através de `recent_change`/`get_routine_changes`, sem criar uma segunda linha persistida. Simplificação técnica registrada para revisão futura, se Jams preferir o comportamento literal de duas linhas.
5. **Número mínimo de evidência (`MIN_EVIDENCE = 3`)** antes de qualquer padrão ser considerado — não fazia parte do que precisava de aprovação (só janelas/limiares de frequência), decidido tecnicamente para nunca promover a partir de 1-2 eventos (`BUSINESS_RULES.md` #3). Documentado em `PATTERN_ENGINE.md`.
5. **Escopo de memória limitado a `memory_entries` (memória permanente)**, conforme o próprio texto do `ROADMAP.md` ("Implementar MEMORY.md: memory_entries, recuperação de contexto relevante"). Memória temporária (camada 2 de `MEMORY.md`, ex.: "hoje estou cansado", sem tabela em `DATABASE.md`) não foi implementada nesta fase.

### 2026-09-08 — FASE 8 (Planejador)

1. **Algoritmo de ordenação de `get_daily_plan` confirmado por Jams (Opção A)**: eventos e tarefas com `due_date` hoje são âncoras cronológicas por período do dia; tarefas sem `due_date` ordenadas primariamente por `priority`, com padrão `active` de `task_time_of_day` como critério adicional de período, `created_at` como desempate final, e tarefas sem período disponível ao final da lista. Resolve o "A DEFINIR" de `PLANNER.md`/`V1_SCOPE.md` ("algoritmo de priorização do Planejador em conflitos"). Implementado em `pattern_engine`-style em `planner/daily_plan.py`.
2. **Sobreposição de compromissos confirmada por Jams**: adotada a OPÇÃO RECOMENDADA já esboçada em `PLANNER.md` — sinalizar, nunca bloquear silenciosamente nem criar por conta própria. Mecanismo técnico: `create_event`/`update_event` devolvem `conflict: true` (tool) ou HTTP 409 (REST) em vez de gravar, e só gravam mesmo com sobreposição com `confirm_overlap: true` explícito. Resolve o "A DEFINIR" de `MODULES/AGENDA.md`/`ACCEPTANCE_CRITERIA.md`.
3. **Efeito de `energy_level: "low"` em `reorganize_day` confirmado por Jams (Opção A)**: tarefas sem horário fixo de maior esforço (`priority = high` e/ou `pomodoro_enabled = true` — únicos campos existentes usáveis como proxy, sem inventar métrica de esforço) vão para o fim da lista, com `suggested_due_date` sugerindo amanhã; eventos fixos e tarefas com `due_date` nunca são tocados; `medium`/`high`/ausente devolvem o mesmo plano de `get_daily_plan`.
4. **Escopo de dado real limitado a `tasks` + `events` + padrões `task_time_of_day`.** Hábitos (sem módulo construído), progresso de `goals` (nunca implementado desde a FASE 2) e um eventual padrão `productivity_time` (nunca existiu — só `task_time_of_day` foi implementado na FASE 7) não são usados — mesmo raciocínio da FASE 7 para o Motor de Padrões. `tools/events.py`/`api/routers/events.py` foram alterados apenas para a sinalização de conflito (item 2), que já era responsabilidade explícita desta fase, não um caso de dado novo alimentando fases anteriores.
5. **FASE 8 ficou backend-only** (Planejador em `planner/daily_plan.py`, tools `get_daily_plan`/`reorganize_day`, endpoints `GET /planner/daily-plan`/`POST /planner/reorganize`), mesmo padrão das FASES 5–7 (`ROADMAP.md` não menciona nenhuma tela para esta fase). `frontend/src/screens/home/HomeScreen.tsx` não foi alterado — seu bloco `DailyPlanBlock` (filtro client-side ingênuo por "é hoje?", construído na FASE 3 antes do Planejador existir) fica **divergente do plano real** assim que a conversa usar `get_daily_plan`; religar a Home ao endpoint `GET /planner/daily-plan` fica registrado como pendência para uma fase/ajuste futuro de frontend.
6. **`get_daily_plan`/`reorganize_day` não entram em `Context` a cada turno** (ao contrário de padrões/memória) — são chamadas só sob demanda pelo LLM, como `get_patterns`/`get_routine_changes` antes de a FASE 7 confirmar o uso proativo. Nem `LLM.md` nem `PLANNER.md` pedem injeção contínua no contexto.
7. **Preferências livres de `memory_entries` não entram no cálculo determinístico da ordenação** — só podem influenciar o que o LLM comenta sobre o plano, nunca a lógica do sistema (`BUSINESS_RULES.md` #7: cálculos pertencem ao sistema).

### 2026-09-09 — FASE 8 (completude / preparo de integração com a FASE 9)

Passada de completude sobre a FASE 8 já commitada, sem mudar nenhum comportamento observável nem o contrato das tools/endpoints. Decidido com Jams antes de implementar (duas escolhas de escopo: "serializer tipado, contrato intacto" e "unificar os quatro caminhos de conclusão de tarefa").

1. **`get_daily_plan` / `reorganize_day` inalterados em comportamento e formato.** O JSON continua `{"date", "items": [...]}` byte-a-byte igual a `TOOLS.md`. A serialização virou ponto único (`planner/serialization.py`), usada pela tool e pelo endpoint; `api/routers/planner.py` passou a devolver o mesmo dict (ainda validado por `response_model=DailyPlanOut`).
2. **`DailyPlan` ganhou campos internos `energy_level` / `reorganized`**, **não serializados** — existem só para a FASE 9 estender o objeto tipado (ex.: anexar expressão do mascote) sem mudar a assinatura. Coberto por teste que trava o contrato de saída.
3. **Utilitário compartilhado de dia/fuso/período: `backend/core/day_window.py`.** `local_today`, `day_bounds`, `period_for_hour`, `PERIOD_ORDER` — extraídos de `planner/daily_plan.py` (antes privados). Motivo: a FASE 9 (streak, "horas na semana", gatilhos do mascote por período) precisa usar exatamente a mesma fronteira de "hoje". `pattern_engine/task_time_of_day.py` **não foi tocado** (Motor de Padrões congelado); a divisão de período fica duplicada de propósito, com os valores idênticos travados por teste (`PERIOD_ORDER == labels de _PERIODS`).
4. **Heurística de "maior esforço" isolada: `backend/core/effort.py`** (`is_high_effort` / `is_high_effort_task`). Mesma regra da FASE 8 (`priority = high` e/ou `pomodoro_enabled`), agora reutilizável pela FASE 9 ("foco total"/Pomodoro em `GAMIFICATION.md`) sem duas definições divergentes.
5. **Ponto único de conclusão de tarefa: `backend/core/task_completion.py`.** Os quatro caminhos que marcavam `tasks.status = done` (`tools/tasks.py:complete_task`, `tools/tasks.py:update_task` com `status=done`, `POST /tasks/{id}/complete`, `PATCH /tasks/{id}` com `status=done`) passam todos por `complete_task`, que dispara o gancho de domínio. Idempotente: não redispara numa tarefa já `done`.
6. **Ganchos de domínio: `backend/core/domain_events.py`** — `on_task_completed`, `on_pomodoro_completed`, `on_low_energy_reported`. **Sem efeito nesta fase (só log)**; nada de XP/nível/conquista/`gamification_*`/`mascot_*` — isso é FASE 9. São o lugar único onde a FASE 9 se liga, para não ter que reabrir tools/routers/Orquestrador. `on_pomodoro_completed` fica **definido mas sem chamador** (não existe módulo de Pomodoro; `pomodoro_sessions` não tem escritor no código).
7. **Sinal observável de baixa energia.** `reorganize_day` (tool **e** endpoint) chama `on_low_energy_reported` quando `energy_level == "low"` — disparado nos dois pontos de entrada, sem depender de qual foi usado, e **sem alterar o corpo da resposta**. FASE 9 usa o sinal para a expressão acolhedora do mascote (`MASCOT.md`: "usuário cansado → expressão acolhedora"). Nenhuma tabela nova (não há tabela de energia; `gamification_*` fora de escopo).
8. **Sem migrations, sem frontend, `pattern_engine/*` intocado.** `frontend/src/screens/home/HomeScreen.tsx` segue como pendência registrada na passada anterior (item 5 de 2026-09-08).

### 2026-09-09 — FASE 9 (Gamificação e Mascote)

Implementada a camada de gamificação + mascote sobre os ganchos da FASE 8. Decisões de produto tomadas por Jams antes da implementação: evolução do mascote **DECISÃO C** (nível dirige o estágio estrutural; conquistas desbloqueiam elementos complementares); não construir módulo de Pomodoro; reutilizar os hooks/utilitários da FASE 8; não quebrar contratos de tool/API.

1. **Valores de produto adotados da OPÇÃO RECOMENDADA de `GAMIFICATION.md`, todos configuráveis.** XP (tarefa +10, bônus alto esforço +5, Pomodoro +20, conquista +25), nível linear (`XP_PER_LEVEL = 100`), catálogo de conquistas (streak 3/7/30, tasks 25/100, pomodoro 1/10/50, focus_day, weekly_hours_5) e limiares nível→estágio (1:1-2, 2:3-5, 3:6-9, 4:10-14, 5:15+) vivem em `backend/gamification/config.py` e `backend/mascot/catalog.py`. Documentados em `GAMIFICATION.md`/`MASCOT.md`. Jams pode ajustar qualquer número sem retrabalho estrutural — registrado como "proposta adotada, ratificável".
2. **Lacuna de `DATABASE.md`: tabela `achievements`.** `GAMIFICATION.md` decide a feature "conquistas", mas o schema até a FASE 8 só previa `gamification_state`/`gamification_events`. Criada `achievements` (`user_id`, `code`, `unlocked_at`, `UNIQUE(user_id, code)`) numa migration nova (`9a1c7f2b5e04`) e acrescentada a `DATABASE.md`. Alternativa rejeitada: sobrecarregar `gamification_events.event_type` com o código da conquista.
3. **Pomodoro não foi construído.** `pomodoro_sessions` continua sem produtor de produção (falta `MODULES/POMODORO.md`, endpoints em `API.md`, tool em `TOOLS.md`). O hook `on_pomodoro_completed` recebeu a lógica de gamificação e é exercitado por teste chamando o gancho direto — quando o módulo existir, a conclusão de sessão entra por ali sem mudança.
4. **Hook `on_habit_logged` não existe.** A OPÇÃO RECOMENDADA de `GAMIFICATION.md` cita "completar hábito do dia", mas não há módulo de Hábitos. `XP_HABIT_LOGGED = 15` fica pronto em `config.py`, sem hook produtor; `habit_logged` já está na lista de eventos que contam para streak.
5. **Despachante de eventos em `core/domain_events.py`.** Os três `on_*` da FASE 8 mantêm nome e assinatura; agora distribuem para subscribers registrados (`subscribe`/`emit`/`_dispatch`). `backend/bootstrap.py` importa `gamification.subscribers` e `mascot.subscribers` (registro idempotente); importado por `api/main.py` e pelos `conftest.py`. **Um subscriber que levanta exceção é logado como `WARNING` e não interrompe a ação de origem** — concluir tarefa funciona mesmo se a gamificação falhar. Trade-off: falha de gamificação é silenciosa para o usuário (aceitável no V1).
6. **Nenhuma lógica de XP/mascote espalhada.** `tools/tasks.py` e `api/routers/tasks.py` **não foram tocados** — os 4 caminhos já convergiam para `core.task_completion` desde a FASE 8. Gamificação e mascote consomem só os eventos de domínio.
7. **`reorganize_day` (tool + endpoint) ganhou `db.commit()` após `on_low_energy_reported`.** O subscriber do mascote persiste `mascot_state` (expressão `caring`); "quem chama é dono da transação" (regra FASE 8). O retorno continua `{"date","items":[...]}` inalterado.
8. **Tool nova `get_gamification_state`** (só leitura) em `tools/registry.py`, e parágrafo novo em `orchestrator/prompt.py` — o LLM comenta nível/XP/streak só com números do sistema, nunca estima (`BUSINESS_RULES.md` #6/#7). Não há tool de escrita de gamificação. Contratos das tools existentes preservados.
9. **Estado do mascote separado da renderização.** `mascot_state.evolution_stage` é cache — `resolve_state` sempre deriva o estágio do nível (DECISÃO C). `current_expression` volta a `idle` após `EXPRESSION_TTL_MIN = 90` min na leitura (não persiste `idle`). `unlocked_features` é derivado de `achievements` na leitura, sem coluna nova. Limitação V1: "último evento vence" para a expressão (uma conclusão de tarefa que também sobe de nível pode terminar em `happy` em vez de `proud`).
10. **Animação do mascote — risco de `MASCOT.md` resolvido.** Stack real (React 19 + Vite, sem lib de animação) → SVG inline + CSS puro (`@keyframes` bob/pulse, `transition`), `prefers-reduced-motion` desliga tudo. Limitação V1 registrada: troca de expressão instantânea (sem morph/tween), mudança de estágio só scale+fade, comemoração sem partículas. Assets finais não entregues (`UX.md`) — SVG de trabalho, trocável sem mexer na lógica (que está no backend).
11. **Home numa passada (pendência da FASE 8 resolvida).** `HomeScreen.tsx`: `DailyPlanBlock` religado a `GET /planner/daily-plan` (fim do filtro client-side "é hoje?"); `ProgressBlock` real (nível, barra de XP, streak, conquistas, `<Mascot>`, seletor de cor). Proxy do Vite ganhou `/planner`, `/gamification`, `/mascot`. Hierarquia da Home mantida.
12. **Sem alteração em `pattern_engine/*`.**

### 2026-09-09 — FASE 10 (Estudos, Casa, Carreira, Hábitos, Pomodoro)

Implementados os cinco módulos V1 que ainda não existiam no código. Notícias e Relatórios seguem fora (V2+). Decisões de produto fixadas por Jams antes da implementação; as decisões técnicas inevitáveis foram propostas como DT-1…DT-14 e adotadas com a implementação.

1. **Estudos e Casa — sem backend próprio.** São `TaskCategory.STUDIES` / `TaskCategory.HOUSE` (já no enum desde a FASE 1). Frontend: `StudiesScreen`/`HouseScreen` são a `TasksScreen` com `categoryFilter` (a tela ganhou props opcionais `categoryFilter`/`title`; sem prop = comportamento de Tarefas idêntico). Rotas `/studies` `/house`, atalhos na nav e na Home. Nenhuma tabela, nenhum CRUD, nenhum endpoint, nenhum campo de categoria em `events` (estudo-compromisso é `event` normal).
2. **Casa — sem pets, sem "responsável".** `tasks` não ganhou campo de responsável (fica para V2/multiusuário). Nenhum model/enum/tela de pets.
3. **Carreira — acompanhamento manual.** Nova tabela `job_applications` (migration `b2d4e6f80a1c`; `down_revision = 9a1c7f2b5e04`). Enum `JobApplicationStatus` (`interested/applied/interviewing/offer/rejected`, valores em inglês como `GoalStatus`; rótulos PT no frontend — DT-10). Endpoints `GET/POST /career/applications`, `PATCH /.../{id}` (sem DELETE, como Objetivos). Tools `create_job_application`/`update_job_application`/`list_job_applications` (DT-11, mantém a regra 1:1 de `API.md`). **Sem** busca de vagas, **sem** LLM pago. `CareerScreen` no frontend.
4. **Hábitos — reutiliza `habits`/`habit_logs`** (sem mudança de schema). `frequency_target` = dias/semana 1–7 (ratifica a decisão técnica da FASE 1 — DT-13). Tools `create_habit`/`list_habits`/`update_habit`/`log_habit` (sem `delete`). Endpoints `GET/POST /habits`, `PATCH /habits/{id}`, `POST /habits/{id}/log`. `habits` não tem `updated_at` — `update_habit` não bumpa timestamp (DT-13).
5. **Streak individual de hábito (DT-1)** em `backend/habits/streak.py`: semanas-calendário consecutivas (seg–dom, via `core.day_window`) com ≥ `frequency_target` logs; semana corrente "em andamento" não zera. **Distinto** do streak global da FASE 9, que **não** foi alterado.
6. **`log_habit` idempotente por dia local (DT-2)** em `backend/habits/service.py`: 2º log no mesmo dia devolve o existente, sem novo `habit_log`, sem novo XP, sem novo `gamification_events`. `habit_logs.context` passa a ser `{"hour","weekday"}` (DT-14) — o `pattern_engine` **não** foi tocado.
7. **Integração Hábitos ↔ gamificação sem regra nova.** `core/domain_events.py` ganhou `HABIT_LOGGED` + `on_habit_logged` (mesmo padrão FASE 8/9; hooks anteriores intactos). `gamification/subscribers.py` credita `config.XP_HABIT_LOGGED` (valor já existente desde a FASE 9) via `engine.apply_habit_logged`. `mascot/subscribers.py` → `happy` (DT-3). `"habit_logged"` já era evento qualificante do streak global desde a FASE 9.
8. **Pomodoro — `pomodoro_sessions` como fonte única (DT-8).** `backend/pomodoro/service.py` (start/pause/resume, uma sessão ativa por usuário — DT-5) + `backend/core/pomodoro_completion.py` (ponto único, análogo a `core/task_completion.py`). Endpoints `POST /pomodoro/sessions`, `GET /pomodoro/sessions/active`, `POST /pomodoro/sessions/{id}/{pause,resume,complete}`. **`productivity_logs` não recebe escritor** (DT-8). Só sessões de foco (DT-7); pausa não descontada do tempo (DT-6). **Sessão válida** = iniciada, 1ª conclusão, `>= MIN_VALID_MINUTES` (1 min, DT-4) → dispara `on_pomodoro_completed` (ligado à gamificação desde a FASE 9, sem XP duplicado). Sessão curta vira `completed` sem XP.
9. **Pomodoro sem tool de LLM (DT-9).** Desvio deliberado da regra 1:1 de `API.md`, registrado aqui e no próprio `API.md` — timer é experiência de UI/tempo real. Frontend: `components/PomodoroTimer.tsx` (SVG/CSS, sem lib), aberto de uma tarefa `pomodoro_enabled` na `TasksScreen`; **sem item de menu**.
10. **`MODULES/POMODORO.md` criado** (não existia). Docs atualizadas: `STUDIES.md`, `HOUSE.md`, `CAREER.md`, `HABITS.md`, `DATABASE.md`, `API.md`, `TOOLS.md`, `NAVIGATION.md`.
11. **Contratos existentes preservados.** `get_daily_plan`/`reorganize_day` (JSON e `serialize_plan` intocados), `tools/tasks.py`, `api/routers/tasks.py`, `planner/*`, `pattern_engine/*`, `core/{day_window,effort,task_completion}.py`, gamificação (regras de XP e streak global) e mascote (regras de expressão/estágio) — **nenhuma alteração**. Todo o resto é aditivo (novos models/routers/tools/telas + props opcionais em `TasksScreen`).
12. **Migrations:** uma só (`b2d4e6f80a1c`, tabela `job_applications`). Nenhuma outra tabela nova; `test_schema.py::EXPECTED_TABLES` += `job_applications`.
13. **Testes:** backend +~60 (streak de hábito, habits API/tools, integração hábito↔gamificação, carreira API/tools, pomodoro completion/API/gamificação, dispatcher); `test_gamification_pomodoro.py` (hook direto, FASE 9) substituído por testes via endpoint. Frontend +~15 (`HabitsScreen`, `CareerScreen`, `StudiesScreen`, `HouseScreen`, `PomodoroTimer`, `TasksScreen` filtrada).
